enum ERoutePath {
  HOME_PAGE = "/",
  LOGIN = "/login",
  DASHBOARD = "/dashboard",
  REALTIME = "/realtime",
  DATA = "/data",
  CHART = "/chart",
  TABLE = "/table",
  SAMPLE = "/sample",
}

export default ERoutePath;
